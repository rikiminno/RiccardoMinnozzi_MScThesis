function tPoint = stage_collocation_0(k, x, u, w)
% function that outputs the time collocation of stage k for the current
% phase

global auxdata

% % get the number of stages and final time
% nStages = auxdata.nStages;
% tauf = w(1);
%
% % set the current stage time point
% tPoint = (tauf/(nStages - 1)) * (k - 1);
tPoint = 0;
end

