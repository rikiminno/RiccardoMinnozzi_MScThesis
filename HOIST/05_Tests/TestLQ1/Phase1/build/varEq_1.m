function PhiDot = varEq_1(t, Phi) 
% Variational equations wrapper function for phase 1,
% automatically generated by the PhaseManager class on 12-Jul-2024 15:10:38
X = Phi(1: 10); 
STM = Phi(11: 110); 
STT = Phi(111: end); 
STM = reshape(STM, 10, 10); 
STT = reshape(STT, 10, 10, 10); 
Xdot = ft_1(t, X); 
ft_X = ft_1_X(t, X); 
ft_XX = ft_1_XX(t, X); 
STMdot = ft_X * STM; 
STTdot = tensorMatrixMultiply(permute(ft_XX, [2 1 3]), STM);
STTdot = tensorMatrixMultiply(permute(STTdot, [3 2 1]), STM);
STTdot = STTdot + tensorMatrixMultiply(STT, ft_X');
STMdot = reshape(STMdot, 100, 1); 
STTdot = reshape(STTdot, 1000, 1); 
PhiDot = [Xdot; STMdot; STTdot]; 
end